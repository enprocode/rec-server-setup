pull_request_rules:
  - name: Dependabot の PR を自動マージ
    conditions:
      - author=dependabot[bot]
      - -conflict
    actions:
      merge:
        method: squash

  - name: マージ済みブランチを削除
    conditions:
      - merged
    actions:
      delete_head_branch: {}
